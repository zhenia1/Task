Console.WriteLine("Введите чило:");
int num = Convert.ToInt32(Console.ReadLine());
 
 if(num % 2 == 1)
 {
    Console.WriteLine("Число" + num + "явлается: Нечётным");
}
else
 {
    Console.WriteLine("Число" + num + "явлется: Чётным");
}
